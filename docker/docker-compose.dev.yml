version: '3.8'

services:
  rabbitmq:
    image: rabbitmq:3.13-management
    container_name: datara-rabbitmq
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_DEFAULT_USER:-datara-producer}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_DEFAULT_PASS:-datara-dev-only}
      RABBITMQ_DEFAULT_VHOST: ${RABBITMQ_DEFAULT_VHOST:-datara}
    ports:
      - "5672:5672"
      - "15672:15672"
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    networks:
      - datara-network
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 10s
      timeout: 5s
      retries: 10
    restart: unless-stopped

  # MongoDB - Document Database
  mongodb:
    image: mongo:7.0
    container_name: datara-mongodb
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: password
      MONGO_INITDB_DATABASE: datara_db
    volumes:
      - mongo_data:/data/db
      - mongo_config:/data/configdb
    networks:
      - datara-network
    healthcheck:
      test: echo 'db.runCommand("ping").ok' | mongosh localhost:27017/test --quiet
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  # Backend - Flask API Server
  backend:
    build:
      context: ..
      dockerfile: docker/Dockerfile-backend
      target: development
    container_name: datara-backend
    ports:
      - "5000:5000"
    env_file:
      - ../config/.env
    environment:
      FLASK_ENV: development
      FLASK_DEBUG: 1
      MONGODB_URI: mongodb://admin:password@mongodb:27017/datara_db
      CELERY_BROKER_URL: amqp://${RABBITMQ_DEFAULT_USER:-datara-producer}:${RABBITMQ_DEFAULT_PASS:-datara-dev-only}@rabbitmq:5672/${RABBITMQ_DEFAULT_VHOST:-datara}
    volumes:
      - ../backend:/app/backend
      - ../uploads:/app/backend/uploads
      - ../dataset:/app/backend/dataset
      - ../models:/app/backend/models
    depends_on:
      mongodb:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    networks:
      - datara-network
    restart: unless-stopped

  # dashboard - React Development Server
  dashboard:
    build:
      context: ..
      dockerfile: Dockerfile-dashboard
      target: development
    container_name: datara-dashboard
    ports:
      - "5173:5173"
      - "8080:8080"
    environment:
      VITE_API_URL: http://localhost:5000
    volumes:
      - ../dashboard:/app/dashboard
      - /app/dashboard/node_modules
    depends_on:
      - backend
    networks:
      - datara-network
    restart: unless-stopped

#  # Robotics Service
#  robotics:
#    build:
#      context: ..
#      dockerfile: docker/Dockerfile
#      target: robotics
#    container_name: datara-robotics
#    ports:
#      - "5002:5002"
#    environment:
#      ROBOTICS_PORT: 5002
#      MONGODB_URI: mongodb://admin:password@mongodb:27017/datara_db
#    volumes:
#      - ../robotics:/app/robotics
#      - ../models:/app/models
#    depends_on:
#      - mongodb
#      - backend
#    networks:
#      - datara-network
#    restart: unless-stopped

  # Nginx Reverse Proxy
  nginx:
    image: nginx:alpine
    container_name: datara-nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/ssl:/etc/nginx/ssl:ro
    depends_on:
      - dashboard
      - backend
    networks:
      - datara-network
    restart: unless-stopped

  # Adminer - Database Web UI (optional, for development)
  adminer:
    image: adminer
    container_name: datara-adminer
    ports:
      - "8081:8080"
    depends_on:
      - mongodb
    networks:
      - datara-network
    restart: unless-stopped
    profiles:
      - tools

networks:
  datara-network:
    driver: bridge

volumes:
  rabbitmq_data:
    driver: local
  mongo_data:
    driver: local
  mongo_config:
    driver: local


